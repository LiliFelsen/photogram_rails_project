<h2>Most Popular Tags:</h2>
<ol>
  <% Tag.most_popular.each do |t|%>
  <li><%= t %></li>
  <% end %>
</ol>


<h2>Most Trending Tag:</h2>
<li><%= Tag.trending %></li>

<h2>Newsfeed:</h2>

<table>
  <% @pictures.reverse_each do |pic|%>
  <tr>
    <td><%= link_to image_tag(pic.image.url(:medium)), picture_path(pic) %></td>
  </tr>
  <% end %>
</table>
