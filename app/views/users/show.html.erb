<h1><%= @user.username %>'s Page</h1>


<%= link_to 'Add a new picture', new_picture_path %><br>
<%= link_to 'Edit User', edit_user_path %><br>
<%= link_to 'Go to homepage', root_path %><br><br>

<table>
  <tr>
    <th>Your pictures:</th>
  </tr>
  <tr>
    <td>
      <% @user.pictures.reverse_each do |pic| %>
        <%= link_to image_tag(pic.image_url, width: "200"), picture_path(pic) %><br>
      <% end %>
    </td>
  </tr>
</table>

<br>

<h3>Your comments:</h3>
<% @user.comments.each do |com| %>
On picture <%= com.picture.title %>:
<%= com.content %> <%= link_to "Delete comment", comment_path(com), method: :delete %> <br>
<% end %>
